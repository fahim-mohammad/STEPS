 Remove mixed-language strings (all UI uses translation keys)
 Make every page use Navbar + PageShell correctly
 Remove WhatsApp automation endpoint (only WhatsApp link remains)
 Remove/redirect legacy receipt page (keep vault + pdf only)
 Remove duplicate/extra routes that do same job
 Confirm dropdown background is solid everywhere
 Confirm auth + admin protection for every admin route
 Final test: build passes and no console errors
